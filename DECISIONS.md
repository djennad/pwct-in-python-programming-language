# DECISIONS — القرارات البرمجية وأسبابها

> جزء من توثيق المشروع:
> [PROJECT_OVERVIEW](PROJECT_OVERVIEW.md) · [ARCHITECTURE](ARCHITECTURE.md) · [CODEBASE_ANALYSIS](CODEBASE_ANALYSIS.md) · [IMPLEMENTATION_PLAN](IMPLEMENTATION_PLAN.md) · [CURRENT_STATE](CURRENT_STATE.md) · [TODO](TODO.md)
>
> أي قرار جديد يُضاف هنا برقم جديد، مع ذكر سببه والبديل الذي رُفض.

---

## 1. المكتبات والتقنيات المختارة

| التقنية | الاستخدام | لماذا |
|---|---|---|
| **Python 3.8+** | لغة المشروع كله | طلب صاحب المشروع، ومتوفرة على كل الأنظمة |
| **tkinter + ttk** (مكتبة قياسية) | كل الواجهة: `ttk.Treeview` للشجرة، و Canvas للتدرج الفضي، و Toplevel للنوافذ | موجودة مع بايثون، فلا يحتاج المستخدم تثبيت أي شيء |
| **json** | ملفات `.pwct` والمكونات و `language.json` والإعدادات | بديل مقروء عن جداول DBF |
| **re / shlex / subprocess / tempfile / textwrap / keyword** | الاستبدال، وتقسيم الأوامر، والتشغيل، والتحقق | مكتبات قياسية |
| **compile()** المدمجة | فحص كود بايثون (VPL Compiler والتحقق من القيم) | فحص دقيق بلا أدوات خارجية |
| **المترجمات الخارجية** | gcc و clang و tcc، و csc.exe، و java و javac، و node | يستخدمها الـ VPL فقط إذا وُجدت، مثل `rpwibuild.bat` في الأصل |
| **Pillow** | **وقت التطوير فقط**: تحويل BMP إلى PNG، وتصغير الأيقونات، ولقطات الشاشة في الاختبار | لا يُستخدم وقت التشغيل |
| **unittest** (مقترح) | اختبارات الوحدة القادمة | قياسية |

**تقنيات رُفضت:**
- **PyQt و PySide:** ثقيلة، وتحتاج تثبيتًا ورخصة.
- **wxPython:** يحتاج تثبيتًا.
- **SQLite:** بيانات المشروع صغيرة وشجرية، و JSON أبسط منه.
- **مكتبة DBF خارجية:** قارئ بسيط (`tools/dbfread.py`) كفى للتحليل.

## 2. القرارات المعمارية والتصميمية

### D1. واجهة Tkinter بلا تبعيات
- **القرار:** كل الواجهة بـ tkinter و ttk فقط.
- **السبب:** التشغيل الفوري على أي جهاز فيه بايثون.
- **العاقبة:** بعض المظهر (التدرجات، والأزرار ذات الأيقونات) مبني يدويًا في `common.py`.

### D2. البرنامج غير DPI-aware، والتخطيط بإحداثيات بكسل
- **السبب:** صفحات التفاعل في الأصل (IDF) إحداثياتها ثابتة، وكذلك رأس Goal Designer. لو جُعل البرنامج DPI-aware لكبرت الخطوط دون الإحداثيات فتتداخل العناصر.
- **العاقبة:** Windows يكبّر النافذة كصورة، فيظهر النص أقل حدة على الشاشات بتكبير 200% (كما في VFP الأصلي).
- **مستقبلًا:** خيار DPI-aware مع ضرب كل الإحداثيات في معامل التكبير.

### D3. JSON بدل DBF، وملف مكوّن واحد بدل TRF و IDF و RULES
- **السبب:** سهل القراءة والتعديل والمقارنة ووضعه في git. ملف واحد يسهّل النقل والتثبيت.

### D4. مفتاح المكوّن = مساره النسبي داخل `components/`
- **السبب:** شجرة المجالات تقابل المجلدات مباشرة، فلا نحتاج جدول مجالات (t34) منفصلًا.
- **العيب:** نقل ملف مكوّن أو إعادة تسميته يكسر الملفات التي تستخدمه. العلاج مخطط في المرحلة 7.

### D5. `internum` = ترتيب سطر `NEWSTEP` في الـ mask، حتى لو كان السطر داخل TEST مستبعد
- **السبب:** يحافظ على هوية كل خطوة عند Modify، فتبقى الخطوات الموجودة بداخلها.
- **مقارنة بالأصل:** الأصل يعدّ أثناء التنفيذ، وفيه خطأ يجعل الترقيم يتغير. انظر [CODEBASE_ANALYSIS §7](CODEBASE_ANALYSIS.md).

### D6. في Modify: الخطوة الجديدة تُدرج قبل أول خطوة شقيقة من نفس التفاعل رقمها أكبر
- **السبب:** الأصل يضيفها في آخر القائمة، فيأتي Else بعد End of If ويفسد ترتيب الكود.

### D7. Modify يحذف الخطوات التي لم تعد تُولَّد، مع ما تحتها
- **السبب:** نفس سلوك الأصل، والكود الناتج يبقى صحيحًا.
- **العاقبة:** إلغاء Else يحذف محتواه. تنبيه المستخدم قبل الحذف فكرة مسجلة في TODO.

### D8. الكود الذي يأتي قبل أول `NEWSTEP` أو في خطوة ليست من التفاعل الحالي يُعلَّق
- **السبب:** الأصل يضيفه للخطوة الأب، فيتكرر مع كل Modify ويعدّل خطوات لا تخص التفاعل.

### D9. نص تعبير TEST يُقص، والقيمة الفارغة تعني "النص فارغ"
- **السبب:** اختبار الحقول الاختيارية الفارغة (مثل Else If) ضروري للمكونات، وفي الأصل لا يعمل بسبب المسافة بعد الأمر.

### D10. `<pwct:tofile>` لا يمسح الكود الرئيسي
- **السبب:** الأصل يفرّغ الناتج عند TOFILE، فيضيع ما قبله. هنا يُحفظ الملف وحده ويكمل الكود الرئيسي.
- **إضافة:** تُزال الإزاحة المشتركة من محتوى الملف، وتُكتب الملفات بجانب الملف الرئيسي، مع منع الكتابة خارج مجلده.

### D11. الكتلة الفارغة تأخذ `language.empty_block`
- القيمة `pass` في بايثون، وفارغة في غيرها.
- أسطر التعليقات لا تُحسب جملًا، فالكتلة التي فيها تعليق فقط تأخذ `pass` أيضًا.

### D12. خاصية `escape` للنصوص
- القيمة لا تُقص، وتُهرَّب فيها `\` و `"`.
- **السبب:** الحفاظ على المسافات في الرسائل مثل `"Your guess : "`، ومنع كسر الكود إذا كتب المستخدم علامة تنصيص.

### D13. التحقق من القيم حسب `kind`
- بايثون: `compile()` بدقة كاملة.
- باقي اللغات: توازن الأقواس وعلامات التنصيص، وصيغة الاسم (identifier).
- **السبب:** تحقيق فكرة Syntax Directed (رفض الأخطاء قبل توليد الكود) دون كتابة محلل لكل لغة.

### D14. حفظ القيم الخام لكل تفاعل
- **السبب:** إعادة تعبئة صفحة التفاعل عند Modify، ودعم "Refresh Steps based on current components" في Time Machine.

### D15. المعرّفات بصيغة `"N_"` وعدّاد واحد للمشروع
- مثل مفاتيح الشجرة في الأصل (`STR(stepsnum) + "_"`)، وجذر كل هدف `SP_N_`، فتكون فريدة عبر كل الأهداف.

### D16. تحسينات على الاستخدام
- **الحذف يطلب تأكيدًا**، والأصل لا يطلبه.
- **Interact مسموح على Start Point**، والأصل يرفضه.
- **Enter على خطوة مُنشأة يدويًا** يفتح Edit Step.
- **النقر المزدوج** ينفّذ Modify أو Edit.

### D17. أوامر اللغة قائمة بدائل تُجرَّب بالترتيب
- مثال: `gcc` ثم `clang` ثم `tcc`.
- الأوامر تُقسَّم بـ `shlex`، وتُزال منها علامات `"`، وتُمرَّر كقائمة وسائط (بدون shell).
- `{exe}` لا يُفحص وجوده، لأنه يُنشأ أثناء البناء.
- **السبب:** يعمل على أجهزة مختلفة دون تعديل، ويتجنب مشاكل المسافات في المسارات.

### D18. التشغيل عبر `runner.py` و `job.json` في Console جديدة
- **السبب:** ملف واحد يدعم البناء والتشغيل لكل اللغات، ويعرض أخطاء البناء، وينتظر Enter حتى لا تُغلق النافذة. يقابل `rpwibuild.bat` في الأصل.

### D19. ناتج الأمثلة الموجودة داخل `languages/` يُكتب في `%APPDATA%\PyPWCT\run`
- **السبب:** حتى لا تمتلئ مجلدات الأمثلة بملفات `.c` و `.exe`.

### D20. `start.pwct` يُبنى بالمحرك نفسه
- مثل Start.SSF في الأصل، ويضمن أن الهيكل (Program و Functions و Main) متوافق مع المكونات.

### D21. المكونات والأمثلة تُولَّد من سكربتات `tools/make_*.py`
- **السبب:** تعديل الـ masks لكل اللغات من مكان واحد، وضمان أن كل الأمثلة مبنية بالمحرك.
- **العيب:** السكربتات تحذف مجلد components وتعيد إنشاءه. العلاج مخطط في المرحلة 7.

### D22. قواعد إضافية: `requires_step` و `allow`
- `requires_step` جديدة: مثلًا Define Function داخل "Functions" فقط.
- `allow` تطبيق لقاعدة `ALLOW:` في الأصل.
- **السبب:** في C و C# و Java لا يجوز تعريف دالة داخل دالة، ولا كتابة جمل خارج الدوال.

### D23. قرارات خاصة بـ C#
- `static Random pwctRandom` في الصنف، لأن `new Random()` داخل حلقة يعطي نفس الأرقام.
- استخدام `var` للإدخال.
- عدم استخدام `$""`، لأن csc في .NET 4 يدعم C# 5 فقط.
- الاعتماد على csc الموجود مع Windows بدل `dotnet`، لأنه لا يحتاج إنشاء مشروع.

### D24. قرارات خاصة بـ Java
- الصنف `Main` غير public، والتشغيل بـ `java File.java` (source launcher)، والإدخال بـ `static Scanner input`.
- **السبب:** لا حاجة لـ javac منفصل، ولا يُشترط أن يطابق اسم الملف اسم الصنف.

### D25. قرارات خاصة بـ JavaScript
- دالة `input()` متزامنة في الـ prelude، تقرأ من `fs.readSync(0)` بايتًا بايتًا، ثم تفك UTF-8 في النهاية.
- **السبب:** Node لا يوفر إدخالًا متزامنًا، وقراءة البايتات ثم فكها معًا تحفظ الحروف العربية.

### D26. قرارات خاصة بـ C
- الـ prelude يضم `stdio` و `stdlib` و `string` و `math` و `time`، و `windows.h` فقط تحت `#ifdef _WIN32`.
- `srand(time(NULL))` في Main.
- النص يُعرَّف كـ `char[256]`، والإدخال بـ `scanf(" %255[^\n]")`.

### D27. اختصارات النافذة الرئيسية بـ `bind` على الجذر مع فحص النافذة
- **السبب:** حتى يحفظ Ctrl+S داخل Component Designer المكوّنَ نفسه، لا البرنامج.

### D28. واجهة إنجليزية كالأصل
- **السبب:** مطابقة الشكل الأصلي. الترجمة العربية مخططة في المرحلة 10.

### D29. الاختبار بالتشغيل الحقيقي
- الأمثلة تُبنى بالمحرك، وتُترجم بالمترجمات الفعلية، وتُشغَّل بإدخال عبر pipe (`tools/test_samples.py`).
- اختبارات الواجهة تمت بمحاكاة المستخدم مع لقطات شاشة (أداة مؤقتة، سيتم نقلها إلى المشروع في المرحلة 7).

### D30. الرخصة GPL v2
- **السبب:** المشروع يستخدم صور PWCT وأيقوناته، وهي GPL، ومشتق من تصميمه.

### D32. Form Designer يعدّل الخطوات، ولا يُنشئ ملف نموذج منفصلًا
- **السبب:** هذا سلوك الأصل: `rpwi.Command12` يجمع خطوات النافذة، ويستخدم الجدول `ld_mymap` لمعرفة أي متغير هو Top و Left وغيرهما، ثم يحدّث التفاعلات.
- **الفائدة:** كل ما يُصمَّم يبقى قابلًا للتعديل بـ Modify، ويظهر في Time Machine، ويعمل مع VPL Compiler والنسخ واللصق.

### D33. الـ mapping داخل ملف المكوّن (`"designer"`)
- الأصل كان يضع الجدول في الكود (`ld_mymap`)، وهنا يوضع في بيانات المكوّن.
- **السبب:** المصمم عام؛ أي لغة تضيف mapping لمكوناتها يصبح المصمم متاحًا لها بلا تغيير في الكود.

### D34. عرض WYSIWYG بعناصر Tkinter حقيقية
- **السبب:** البرنامج المولَّد يستخدم Tkinter أيضًا، فالشكل في المصمم مطابق للتشغيل: الخطوط، والأحجام التلقائية، وخلفية Label الافتراضية.
- **العاقبة:** للغات أخرى (WinForms و Swing) سيكون العرض تقريبيًا.

### D35. ربط العناصر بالنافذة بالاسم (خاصية `window`)، وليس بمكانها في الشجرة
- **السبب:** مكونات بايثون تضع عناصر النافذة كخطوات شقيقة للنافذة، وليس كأبناء لها. والمستخدم قد ينظّم الخطوات داخل خطوات تنظيمية.

### D36. العناصر الجديدة تُدرج بعد آخر عنصر للنافذة تحت نفس الأب
- **السبب:** لتأتي قبل `mainloop`. الإضافة في آخر القائمة كانت ستضعها بعد Event Loop، فلا تظهر.
- **دالة الحدث:** تُدرج **قبل** النافذة، لأن `command=` يحتاج أن تكون الدالة معرّفة قبل إنشاء الزر.

### D37. مكونات الواجهة بخطوة `NEWSTEP` واحدة، والفروق في أسطر الكود عبر TEST
- مثال: Button مع أمر On Click أو بدونه.
- الموقع يُكتب بـ `place(x, y)`، والعرض والارتفاع عبر `place_configure` فقط إذا كانا غير فارغين.
- **السبب:** تبقى هوية الخطوة ثابتة عند تعديل المصمم لها. ولا يظهر خطأ إذا وُجد العرض دون الارتفاع أو العكس.

### D38. تحسين موضع الإدراج في Modify (`_insert_index`)
- الخطوة الجديدة تُدرج قبل أول خطوة من نفس التفاعل رقمها أكبر، وإن لم توجد فبعد آخر خطوة منه.
- **السبب:** إذا تبدّلت الخطوة الجذرية بين بديلين (مثل Call Function بنتيجة أو بدونها) كانت تذهب إلى آخر القائمة. الآن تبقى في مكانها.

### D39. Ctrl+F لـ Form Designer (كالأصل)، والبحث على Ctrl+Shift+F و F3
- **السبب:** مطابقة اختصار الأصل (`rpwi.KeyPress`: الرقم 6 = Command12).

### D31. تنظيم التوثيق في سبعة ملفات
- الملفات: OVERVIEW و ARCHITECTURE و CODEBASE_ANALYSIS و IMPLEMENTATION_PLAN و CURRENT_STATE و DECISIONS و TODO، و `DOCUMENTATION.md` فهرس لها.
- **السبب:** طلب صاحب المشروع، لمتابعة العمل في جلسات جديدة دون إعادة الشرح.

### D40. Create Window كتلة: Start Here + End of Window (فيها `mainloop`)، مع خيار Show
- **المشكلة:** كانت النافذة تحتاج مكوّنًا منفصلًا (Start Event Loop). من يضيف Create Window وحده يحصل على برنامج ينتهي فورًا بلا نافذة.
- **الحل:** نفس أسلوب PWCT الأصلي (Define Window … End Window ثم Activate Window): العناصر داخل Start Here، و End of Window يولّد `win1.mainloop()` إذا كان Show مفعّلًا. القسم `designer` فيه `"container": 2` (رقم internum لخطوة Start Here).
- **التوافق:**
  - Start Event Loop بقي موجودًا للملفات القديمة، واستدعاء `mainloop()` مرتين لا يسبب خطأ (الثاني يعود فورًا بعد إغلاق النافذة؛ تم التحقق).
  - `FormModel.upgrade_windows()` يرقّي النوافذ القديمة عند فتح الملف: مع Event Loop ← Show = 0، وبدونه ← Show = 1 ونقل العناصر إلى Start Here.
  - `FormModel.controls_place()`: إذا لم توجد خطوة الحاوية (ملف قديم)، يعود المصمم للسلوك القديم (بعد آخر عنصر، قبل Event Loop).

### D41. شجرة مجالات متداخلة مثل HarbourPWCT، وجذرها اسم اللغة المرئية
- **طلب صاحب المشروع:** ترتيب Components Browser كما في الأصل: User Interface ثم GUI Application ثم Windows و Controls، ثم Programming Basics، ثم Programming Paradigm.
- **التنفيذ:** `comp()` في أدوات التوليد ما زال يستخدم اسم المجال القصير، و `tree_path()` و `TREE` و `TREE_ORDER` في `make_components.py` تحدد مجلده في الشجرة. `write_order()` يكتب `_order.txt` في كل مستوى.
- **الشكل:** `ClassicTree` مرسومة على Canvas، لأن `ttk.Treeview` لا يرسم خطوط الشجرة ومربعات +/-. وقائمة المكونات بخط Times New Roman، كما في خصائص `List1` في `selser.scx`.
- **المجالات بلا مكونات لا تظهر:** مجالات الأصل مثل Main Menu و Sound و Logic لا تظهر حتى تُضاف لها مكونات.
- **التوافق:** مفتاح المكوّن هو مساره (مثل `GUI (Tkinter)/Create_Window`)، فتغيّرت المفاتيح. `Library.get()` يبحث باسم الملف إذا لم يجد المفتاح (أسماء الملفات فريدة في كل لغة، ويتحقق منها اختبار)، و `Library.update_keys()` في `App.set_project` يكتب المفاتيح الجديدة. هذا ينفّذ D4 (البحث بالاسم عند فقد المفتاح).

### D42. أدوات التوليد تحذف ملفاتها فقط (`_generated.txt`)
- **السبب:** `write_components()` كان يحذف مجلد المكونات كله، ومعه مكونات المستخدم (`print` و `printo`).
- **الحل:** قائمة بالملفات المولَّدة في `_generated.txt`، ولا يُحذف غيرها. وفي أول تشغيل بدون القائمة، يُحذف فقط ما قيمة `order` فيه أقل من 1000، لأن مكونات Component Designer تُحفظ بـ 1000. والمجلدات التي تفرغ تُحذف.
